import { Row } from "@tanstack/react-table";
import { ContainerType } from "../../schema/containers";

export default function AddDescendant({ row }: { row: Row<ContainerType> }) {
  return <div>Add Descendant</div>;
}
